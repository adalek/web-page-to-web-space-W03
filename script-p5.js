function setup() {
  let canvas = createCanvas(400, 500);
  canvas.parent("container-p5");
  canvas.hide();
  background(100);

  initThree();
}

function draw() {
  //noLoop();
  background(0, 0, 255);
}